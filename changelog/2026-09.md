# September 2026

## Explicit AWS-observed TEST recovery — September 12, 2026 Central Time

- Added a separate TEST-only `AWS-live-snapshot/v1` driver with read-only observation/planning, opt-in private immutable metadata capture and guarded code-only recovery using the original version-specific S3 ZIP.
- Bound the reviewed historical seven-file payload to its exact source, ZIP, version, selector and file digests without changing the current eight-file verifier or inventing original GitHub provenance.
- Pinned current rollback tooling separately from historical payload source. The strict GitHub mode reads historical source only as data and explicitly rejects expired or mismatched artifacts; AWS recovery is separately selected and consumes only private Environment selection plus approved hashes.
- Added executable driver, workflow and regression tests for substitution, unsafe ZIP entries, two-read drift, current-tooling handoff, parameter/resource preservation, exact change-set review and post-recovery readiness.
- Implementation and verification are local only. No snapshot was published, workflow dispatched, IAM changed, AWS resource mutated, draft activated or production changed. Operational use still requires the actual TEST principal's essential read permissions and an approved private recovery record.

## TEST delivery and immutable rollback hardening — September 4, 2026 Central Time

- Preserved the existing exact `dev`-to-`test` promotion and production artifact contract while extending TEST artifact retention to 30 days.
- Added post-deploy stack and Lambda readiness smoke checks plus recorded run, artifact, source SHA, and manifest-digest coordinates for rollback.
- Added a manual TEST rollback workflow that verifies the successful source deployment, checks out its exact source SHA, downloads its artifact by immutable ID, verifies the exact source-bound SAM inventory, and executes only a reviewed nonreplacement change set.
- Added fail-closed parameter preparation and reusable change-set review helpers; remove and replacement actions are rejected.
- Verification passed with 214 tests, Actionlint, Python and Bash syntax checks, SAM lint, and diff validation. No workflow was dispatched and no AWS, production, draft, or Zoosite state changed.
