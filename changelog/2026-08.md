# August 2026

## Commerce published-policy contract alignment — August 24, 2026 Central Time

- Replaced legacy `operatorPauses` and `proration` authoring fields with the current required `planChangePolicy` and closed enabled/disabled `pausePolicy` forms consumed by Commerce.
- Added the optional closed `taxPolicy` and bounded `migrationPolicy` forms, optional bounded uppercase `shipping.allowedCountries`, and the code-owned `subscription:migration:execute` capability.
- Added the consumer invariant that tracked inventory requires inventory enablement.
- Updated the golden descriptor and added bidirectional contract, field-matrix, pre-storage, end-to-end publication, historical-package rejection, and pointer-stability regressions.
- Recorded the release schema as 9,978 normalized-LF bytes at SHA-256 `1F1B2934FBBF025ABC3C1CB7D70FAF2EAB4D856DFC7CCA6461E53037A4E3B6CE`; hub and draft-template copies must match before promotion.
- No AWS write, deployment, branch promotion, or authorization-object change was performed during local implementation.

## Route-language write and publication validation — August 24, 2026 Central Time

- Added an optional route `language` contract for fixed-language public paths while leaving language-free routes backward compatible.
- Required authored route languages to be canonical and present in `site.i18n.supportedLanguages`, supporting both string entries and objects with a `code`.
- Aligned route locale parsing with Runtime's restricted BCP 47 grammar, including canonical script/region casing and rejection of underscores or long language names such as `english`, without rewriting supported-language source bytes.
- Rejected duplicate `(pageId, language)` siblings while accepting distinct language siblings for one page package.
- Applied the same non-mutating validation before version storage and again when a checksum-consistent immutable package is published, so invalid historical payloads cannot move the published pointer.
- Made site metadata derivation select only exact `{domain}/site-config.json` and reject nested or case-altered `site-config.json` candidates before storage or publication, while leaving unrelated server files unchanged.
- Added regression coverage for create, upsert, draft and published retrieval, unchanged payload shape, rejection before S3/DynamoDB writes, and publication-pointer stability.
- Local verification completed with 162 passing unit tests, Python compilation, a reproducible seven-file Lambda build, exact artifact verification, and clean diff checks. SAM CLI and actionlint were not available locally, so no local result is claimed for those optional tools; the repository workflows were left unchanged.
- No AWS write, deployment, branch promotion, or authorization-object change was performed during local implementation.
